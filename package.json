{
  "name": "blog.leonklute.nl",
  "version": "0.0.1",
  "description": "Blog website of Leon Klute",
  "keywords": "blog,java,dev,software",
  "license": "MIT License",
  "author": "Leon Klute",
  "source": "index.html",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && parcel build index.html blog/*.html",
    "dev": "parcel index.html blog/*.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.8.1",
    "rimraf": "^5.0.5"
  }
}
